[pytest]
addopts = -m "not governed_data and not scientific_replay"
markers =
    governed_data: requires separately acquired governed VisA source bytes
    scientific_replay: performs full scientific reproduction using governed data and the documented environment

